# KoʻzNur — Phase-B fine-tune (ARCHITECTURE.md §4)
# Python 3.11+ (works on 3.13). Install into the SAME venv as the backend, or a
# fresh one. The backend already pins torch/torchvision/pillow/opencv/numpy
# (backend/requirements.txt); the lines below repeat the floors that the trainer
# needs plus the train-only deps (timm, scikit-learn, pandas, kaggle).
#
# Quick start:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r backend/requirements.txt -r backend/train/requirements.txt

# --- core training stack ---
torch>=2.2                 # MPS (Apple Silicon) / CUDA / CPU — ARCHITECTURE.md §5
torchvision>=0.17          # transforms + ToTensor/Normalize
timm>=0.9.12               # EfficientNet backbones (tf_efficientnet_b0_ns / b3_ns)

# --- data + metrics ---
scikit-learn>=1.3          # stratified split + QWK (cohen_kappa) + ROC-AUC
pandas>=2.0                # read train.csv (id_code, diagnosis)
numpy>=1.24

# --- image io / preprocessing (shared with the backend serving path) ---
pillow>=10.0
opencv-python-headless>=4.8  # retina crop + optional Ben Graham / CLAHE

# --- data acquisition ---
kaggle>=1.6                # `kaggle competitions download -c aptos2019-...`

# --- optional: Hugging Face fallback loader (--hf-dataset) ---
# Only needed if you skip the Kaggle download and use the HF repackage instead.
datasets>=2.16
huggingface-hub>=0.20
